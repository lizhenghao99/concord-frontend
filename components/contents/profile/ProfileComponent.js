import ProfileFormik from './ProfileFormik';

const ProfileComponent = (props) => {
    return (
        <>
            <ProfileFormik minW={'20rem'}/>
        </>
    );
};

export default ProfileComponent;