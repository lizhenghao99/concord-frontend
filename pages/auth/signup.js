import { SignupComponent } from '../../components/auth/signup/SignupComponent';

const Signup = (props) => {
    return (
        <>
            <SignupComponent/>
        </>
    );
};

export default Signup;